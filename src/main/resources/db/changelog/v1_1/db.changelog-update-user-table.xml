<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.21.xsd">

    <changeSet id="5-add-chat_id-and-state" author="vladimir">
        
        <addColumn tableName="users">
            <column name="tg_chat_id" type="BIGINT"/>
        </addColumn>
        
        <addColumn tableName="users">
            <column name="state" type="VARCHAR(20)"/>
        </addColumn>

        <!-- Ограничение на возможные значения в колонке state -->
        <sql>
            ALTER TABLE users
            ADD CONSTRAINT check_users_state
            CHECK (state IN ('IDLE', 'AWAITING_CHAT_NAME'));
        </sql>
    </changeSet>

    <changeSet id="6-rename-telegram_id-column-in-users" author="vladimir">
        <renameColumn tableName="users" oldColumnName="telegram_id" newColumnName="tg_id" columnDataType="BIGINT"/>
    </changeSet>

    <changeSet id="7-create-chat-table" author="vladimir">
        <!-- Создаём таблицу чатов -->
        <createTable tableName="chats">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_chat_user"
                             referencedTableName="users" referencedColumnNames="tg_id"/>
            </column>

            <column name="name" type="VARCHAR(64)">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="VARCHAR(16)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP"/>
        </createTable>

        <createIndex tableName="chats" indexName="idx_chats_user_id">
            <column name="user_id"/>
        </createIndex>
    </changeSet>

    <changeSet id="8-change-relation-in-messages-from-users-to-chats" author="vladimir">
        <dropForeignKeyConstraint
                baseTableName="messages"
                constraintName="fk_messages_users"/>

        <dropColumn tableName="messages" columnName="telegram_id"/>


        <addColumn tableName="messages">
            <column name="chat_id" type="BIGINT"/>
        </addColumn>

        <addForeignKeyConstraint
                baseTableName="messages"
                baseColumnNames="chat_id"
                constraintName="fk_messages_chat"
                referencedTableName="chats"
                referencedColumnNames="id"
                onDelete="CASCADE"/>
    </changeSet>

</databaseChangeLog>
